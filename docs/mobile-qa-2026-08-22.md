# 行動裝置 QA 與無障礙 — 2026-08-22（DP-032 第二輪，真機）

對象：staging `https://yoyocadence.github.io/DayPop/`，deploy run `32570770842`（head `f7dd158`，v0.3.0）。

裝置：**iPhone 15 Pro Max**（動態島機型），iOS Safari 與「加到主畫面」兩種模式。由專案擁有者實機操作，agent 只負責出題、量測與判讀。

> 第一輪（模擬環境）的報告是 [`mobile-qa-2026-08-13.md`](mobile-qa-2026-08-13.md)。那一輪明確標示「不含真實裝置」，本輪補的就是它 §4 列的部分 —— **但沒有補完**，見 §3。

---

## 0. 前置：staging 先推進到當時的 main

開工時 staging 停在 `3f404d03`（2026-08-16），落後 `main` 三次合併（DP-035、DP-056、DP-023）。由專案擁有者手動 dispatch 部署到 `f7dd158` 後才開始 QA，因此本輪測的就是當時 `main` 的內容。

確認方式不是看 run 綠燈，而是比對線上 bundle 的字串：`匯出資料`／`資料備份`／`匯入 .ics`（DP-056）與 `驗證連結已寄`（DP-023）在舊 bundle 都是 0 次、在新 bundle 都是 1 次。

**版號仍是 0.3.0**，`version.json` 與 `sw.js` 內容不變，因此沒有回寫已部署 release note 的問題。附帶的已知後果：service worker 的 cache 名稱是 `daypop-app-shell-0.3.0`，同版號重新部署不會觸發更新提示，離線時供應的仍是舊的 app shell。線上時導覽請求是 network-first（`sw.js` 的 `fetch` handler），所以本輪不受影響。

---

## 1. 通過的項目

| 項目 | 結果 |
| --- | --- |
| 動態島／狀態列不遮住內容 | **通過。**日曆 header 的 `2026 / 8 / 22 週六` 與 `2026年 8月` 完整落在狀態列下方，`.dp-viewport` 的 `padding-top: env(safe-area-inset-top)`（`shell.css` 65）在真機確實生效。第一輪量不到這一項 —— 模擬環境的安全區恆為 `0px`。 |
| home indicator 不壓住底部分頁列 | **通過。**四個分頁的圖示與文字下方仍有明顯空間，`shell.css` 125／128 的 `env(safe-area-inset-bottom)` 生效。依據是 standalone 截圖，非逐像素量測。 |
| 加到主畫面的圖示外觀 | **通過（專案擁有者實機確認）。**無黑底、白框或裁切。**沒有截圖佐證** —— 這一項的驗收依據是擁有者的目視回報，不是 agent 量測。`index.html:18` 那句「iOS 會忽略 SVG 並把透明區域合成到黑色」的註解，至此才第一次在真機上得到印證。 |
| standalone 啟動外觀 | **通過。**從主畫面圖示啟動後完全沒有 Safari 網址列，manifest 的 `display` 在真機生效。 |

---

## 2. 發現的問題

### 2.1 頁面畫布的底色是寫死的紫，永遠不隨主題變（中）→ DP-074

狀態列那一段有一條 **`#f7f3ff` 淡紫色帶**，切換到深色主題也不變。iOS Safari 與加到主畫面兩種模式表現相同。

以 Playwright 對線上 staging 量測（430×932、DPR 3、`zh-TW`／`Asia/Taipei`），四個分頁 × 淺／深色：

| 層 | 淺色 | 深色 |
| --- | --- | --- |
| `html`（畫布） | `rgb(247,243,255)` | `rgb(247,243,255)` ← **沒變** |
| `.dp-preview` | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `.dp-viewport` | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `.dp-screen`（作用中分頁） | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `theme-color` meta | `#ffffff` | `#121212` |

App 自己的三層背景與 `theme-color` **全部正確**，壞的只有畫布這一層。

**根因是 specificity，不是載入順序。** 根元素上有三條背景規則同時存在：

| 來源 | 選擇器 | 值 |
| --- | --- | --- |
| `shell.css` 20–24 | `html` | `#0a0a0c`（**本來想要的**） |
| `styles.css` 1–3 | `:root` | `#f7f3ff` |
| `styles.css` 26–31 | `html` | 漸層 ＋ `#f7f3ff` |

`:root` 的 specificity 是 `(0,1,0)`、`html` 是 `(0,0,1)`，所以 **`:root` 恆勝，`shell.css` 那條是死碼**。它上面那句註解「App viewport 會自己畫主題背景」只在 `.dp-preview` 的盒子範圍內成立，盒子外的畫布是 canonical 搬移前 scaffold 樣式表的紫。

**成因未確認的部分**：畫布為什麼會在安全區那一段露出來，**這一輪沒有查清楚，不得寫成已確認**。模擬環境重現不了 —— Chromium 的 `env(safe-area-inset-*)` 恆為 `0px`，沒有那段區域可看。最合理的機制是 `overscroll-behavior: none` 只下在 `body`、`html` 算出來是 `auto`（`shell.css` 28–33），iOS 的橡皮筋回彈因此露出畫布，但這是**假設，沒有證據**。

**修法不依賴知道機制**：只要畫布顏色接上主題，不管從哪條路徑露出來，露出的都會是主題色。

**過程中被證偽的一個假設，留著避免下一輪重走**：原本以為差異出在 `.cal-screen { overflow: hidden }`（`calendar.css` 9–13）讓日曆分頁不是捲動容器。**那條規則其實沒有生效** —— `.dp-screen { overflow-y: auto }`（`shell.css` 97–105）同 specificity 但在後面載入，日曆分頁算出來也是 `auto`。只讀原始碼會得到相反結論，必須看計算值。這條 dead rule 目前沒有可見症狀（`.cal-screen` 的 `scrollHeight` 等於 `clientHeight`），但屬同一類遺留，已一併記進 DP-074。

### 2.2 快速新增不吃中文數字的時間（中）→ DP-075（產品決策）

真機打「`明天下午三點 開會`」建出的是**全天**事件，標題還留著「三點 開會」。

以專案 vitest 直接餵 `parseQuickAdd()` 確認（`now = 2026-08-22T12:00:00+08:00`）：

| 輸入 | 標題 | 全天 | 開始 |
| --- | --- | --- | --- |
| `明天下午3點 開會` | `開會` | 否 | 15:00 |
| `明天下午三點 開會` | `三點 開會` | **是** | 09:00 |
| `明天下午十點 開會` | `十點 開會` | **是** | 09:00 |
| `明天15點 開會` | `開會` | 否 | 15:00 |
| `明天下午3:00 開會` | `開會` | 否 | 15:00 |
| `明天下午3點 開會 @會議室A 提前15分` | `開會`（地點 `會議室A`、提前 15 分） | 否 | 15:00 |

根因是時間正則 `(\d{1,2})\s*[:：點点時时]\s*(\d{1,2})?`（`quickAdd.ts:122`）的 `\d` 只吃 ASCII 數字：比對不到 → `hour` 留在 `null` → `allDay = hour === null`（`quickAdd.ts:138`）判成全天，而「三點」沒被時間規則吃掉就留在標題裡。兩個症狀同一個根因。

**這是忠實移植，不是搬移時弄壞的。** 原檔 `.dc.html:974` 是逐字相同的正則、`:979` 是逐字相同的 `allDay = hh==null`。依專案規則（同 DP-015／DP-064／DP-070）另立任務，不在 QA 分支改。

**週檢視看不到這筆事件是正確行為**，不是第二個 bug —— 原稿 `buildWeek()` 的 `evs.forEach(e=>{ if(e.allDay) return; ... })` 本來就略過全天事件，DayPop 的 `WeekView.tsx` 保留同樣行為，tasks.md 的 DP-014 條目已記。

### 2.3 只給時間時標題是空的，儲存會靜默失效（中）→ DP-076（偏離原稿）

比對原稿時順帶量到的，不是真機操作發現的。

`明天下午3點`、`明天`、`下午3點` 三種輸入，`parseQuickAdd()` 回傳的 `title` 都是 `""`。原檔是 `const title = str.trim() || '新事件';`（`.dc.html:983`），`quickAdd.ts:142` 沒有那個 fallback —— 與 §2.2 相反，這一條是**搬移缺口**，原稿才是 canonical。

實際症狀比「少一個預設字串」嚴重：DP-060 之後快速新增交由事件 sheet 確認建立，而 `EventSheet.tsx:162` 的 `if (!title.trim()) return;` 是**沒有任何訊息的 early return**，使用者按儲存會什麼都不發生也得不到解釋。

### 2.4 週檢視拖曳在真機觸控上沒有即時視覺回饋（中）→ DP-077

按住色塊拖動時**色塊不跟著手指**，放開才突然跳到新位置。iOS Safari 與加到主畫面兩種模式相同。

**存下來的時間是正確的**，也**不會誤觸整頁捲動**（兩者皆由專案擁有者實測確認）。所以這是回饋問題，不是資料問題。

**模擬環境三條路徑都重現不了**：

| 路徑 | 拖曳中色塊 `top` | 標籤 |
| --- | --- | --- |
| 桌面滑鼠（1280×900） | 651 → **706**（放開前就動） | `15:00` → `16:15` |
| 模擬觸控（CDP `Input.dispatchTouchEvent`，430×932） | 584 → **639**（放開前就動） | `15:00` → `16:15` |
| **真機觸控** | **不動** | — |

這也解釋了為什麼 6 個 Playwright e2e 從來沒抓到 —— 合成觸控事件不會走 iOS 的手勢仲裁與 implicit pointer capture。

**可以從程式碼證明的部分**：`onUp` 要走到 `onUpdateEvent()` 必須 `drag.moved === true`（只有 `onMove` 會設它）**且** `preview` 已設成該事件（`WeekView.tsx:215-228`）。既然時間有正確改變，代表 **`pointermove` 有觸發、`setPreview` 有跑**；若完全收不到移動事件，`drag.moved` 會是 false，結果會是「開啟事件」而不是改時間。**因此壞的不是事件傳遞，是畫面沒有在手勢進行中重繪。**

**成因未在真機確認。** 最合理的候選是預覽以 inline `style={{ top, height }}` 上到 DOM（`WeekView.tsx:319-324`）—— 那是 layout 屬性，iOS Safari 在觸控手勢進行中通常沿用手勢開始前的合成圖層，要等手勢結束才重繪 layout 變更。**不要直接改成 `transform` 就宣稱修好**：這條的難處正是本機三條路徑都是綠的，必須先在真機證實候選。

### 2.5 橫向沒有版面，垂直的框把內容擠掉一半以上（中）→ DP-078

以 932×430（iPhone 15 Pro Max 橫向）量測：

| | 直向 430×932 | 橫向 932×430 |
| --- | --- | --- |
| 水平溢出（四個分頁） | 0px | 0px |
| 桌面展示框 | 未套用 | 未套用（正確） |
| `.cal-header` 高度 | 181px | **181px（完全沒變）** |
| 底部分頁列 | 64px | 64px |
| **月格可視高度** | **658px** | **156px** |
| 日期格高度 | 109px | 58px |

不是縮放問題 —— 水平方向是好的。問題是 header 181px ＋ 分頁列 64px ＝ 245px 佔掉 430px 的 **57%**，月格只剩約一列半。

根因是專案**完全沒有 orientation 處理**：`src/` 與 `index.html` 裡 `orientation`／`landscape` 出現 **0** 次，唯一斷點是 `shell.css:168` 的桌面展示框（門檻 `min-width: 900px` 且 `hover: hover` 且 `pointer: fine`，橫向寬度過了但觸控裝置不符後兩項，所以正確地沒套用）。

原稿是手機直式原型，**沒有橫向可對照** —— 橫向要長什麼樣是新的產品決策。

### 2.6 轉橫再轉回直向，畫面停在放大狀態（中）→ DP-079

專案擁有者實機回報，**沒有 agent 量測佐證**：Chromium 用 `setViewportSize()` 來回切換不會產生殘留縮放，這是 iOS Safari 旋轉時自行調整縮放後不還原的既有行為，需要真機或 iOS 模擬器才驗得到。

**記下一條約束，避免下一輪用錯方法修**：**不得用 `maximum-scale=1` 或 `user-scalable=no`**。目前的 viewport meta 刻意沒有封鎖縮放，而「使用者縮放沒有被封鎖」是第一輪已通過的 WCAG 項目（[`mobile-qa-2026-08-13.md`](mobile-qa-2026-08-13.md) §1）；加上那兩個值等於用一個無障礙缺陷換一個視覺缺陷。

很可能與 §2.5 是同一件事的兩面 —— 橫向沒有版面，iOS 才會去縮。建議先做 DP-078 再回頭看這條。

---

## 3. 這一輪沒有涵蓋的部分（DP-032 仍未完成）

以下第一輪 §4 列出的項目**本輪未測**，如實記錄，不得視為通過：

- [ ] **Android Chrome**：安裝橫幅、maskable 圖示在啟動器的裁切、返回鍵行為。**專案擁有者手邊沒有 Android 裝置**，需另覓裝置。
- [ ] **螢幕閱讀器（VoiceOver／TalkBack）**：專案擁有者決定本輪先跳過，後期再補。DP-069（roving tabindex）與 DP-071（`main`／`h1`）都是為螢幕閱讀器做的修正，**至今只在模擬環境驗過**，沒有任何真實螢幕閱讀器走訪過。
- [ ] **實體鍵盤／輔助輸入**：無外接鍵盤。

完成前 DP-032 維持 In Progress。
