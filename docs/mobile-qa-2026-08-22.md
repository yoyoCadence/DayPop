# 行動裝置 QA 與無障礙 — 2026-08-22（DP-032 第二輪，真機）

對象：staging `https://yoyocadence.github.io/DayPop/`，deploy run `32570770842`（head `f7dd158`，v0.3.0）。

裝置：**iPhone 15 Pro Max**（動態島機型），iOS Safari 與「加到主畫面」兩種模式。由專案擁有者實機操作，agent 只負責出題、量測與判讀。

> 第一輪（模擬環境）的報告是 [`mobile-qa-2026-08-13.md`](mobile-qa-2026-08-13.md)。那一輪明確標示「不含真實裝置」，本輪補的就是它 §4 列的部分 —— **但沒有補完**，見 §3。

---

## 0. 前置：staging 先推進到當時的 main

開工時 staging 停在 `3f404d03`（2026-08-16），落後 `main` 三次合併（DP-035、DP-056、DP-023）。由專案擁有者手動 dispatch 部署到 `f7dd158` 後才開始 QA，因此本輪測的就是當時 `main` 的內容。

確認方式不是看 run 綠燈，而是比對線上 bundle 的字串：`匯出資料`／`資料備份`／`匯入 .ics`（DP-056）與 `驗證連結已寄`（DP-023）在舊 bundle 都是 0 次、在新 bundle 都是 1 次。

**版號仍是 0.3.0**，`version.json` 與 `sw.js` 內容不變，因此沒有回寫已部署 release note 的問題。附帶的已知後果：service worker 的 cache 名稱是 `daypop-app-shell-0.3.0`，同版號重新部署不會觸發更新提示，離線時供應的仍是舊的 app shell。線上時導覽請求是 network-first（`sw.js` 的 `fetch` handler），所以本輪不受影響。

---

## 1. 通過的項目

| 項目 | 結果 |
| --- | --- |
| 動態島／狀態列不遮住內容 | **通過。**日曆 header 的 `2026 / 8 / 22 週六` 與 `2026年 8月` 完整落在狀態列下方，`.dp-viewport` 的 `padding-top: env(safe-area-inset-top)`（`shell.css` 65）在真機確實生效。第一輪量不到這一項 —— 模擬環境的安全區恆為 `0px`。 |

---

## 2. 發現的問題

### 2.1 頁面畫布的底色是寫死的紫，永遠不隨主題變（中）→ DP-074

狀態列那一段有一條 **`#f7f3ff` 淡紫色帶**，切換到深色主題也不變。iOS Safari 與加到主畫面兩種模式表現相同。

以 Playwright 對線上 staging 量測（430×932、DPR 3、`zh-TW`／`Asia/Taipei`），四個分頁 × 淺／深色：

| 層 | 淺色 | 深色 |
| --- | --- | --- |
| `html`（畫布） | `rgb(247,243,255)` | `rgb(247,243,255)` ← **沒變** |
| `.dp-preview` | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `.dp-viewport` | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `.dp-screen`（作用中分頁） | `rgb(255,255,255)` | `rgb(18,18,18)` |
| `theme-color` meta | `#ffffff` | `#121212` |

App 自己的三層背景與 `theme-color` **全部正確**，壞的只有畫布這一層。

**根因是 specificity，不是載入順序。** 根元素上有三條背景規則同時存在：

| 來源 | 選擇器 | 值 |
| --- | --- | --- |
| `shell.css` 20–24 | `html` | `#0a0a0c`（**本來想要的**） |
| `styles.css` 1–3 | `:root` | `#f7f3ff` |
| `styles.css` 26–31 | `html` | 漸層 ＋ `#f7f3ff` |

`:root` 的 specificity 是 `(0,1,0)`、`html` 是 `(0,0,1)`，所以 **`:root` 恆勝，`shell.css` 那條是死碼**。它上面那句註解「App viewport 會自己畫主題背景」只在 `.dp-preview` 的盒子範圍內成立，盒子外的畫布是 canonical 搬移前 scaffold 樣式表的紫。

**成因未確認的部分**：畫布為什麼會在安全區那一段露出來，**這一輪沒有查清楚，不得寫成已確認**。模擬環境重現不了 —— Chromium 的 `env(safe-area-inset-*)` 恆為 `0px`，沒有那段區域可看。最合理的機制是 `overscroll-behavior: none` 只下在 `body`、`html` 算出來是 `auto`（`shell.css` 28–33），iOS 的橡皮筋回彈因此露出畫布，但這是**假設，沒有證據**。

**修法不依賴知道機制**：只要畫布顏色接上主題，不管從哪條路徑露出來，露出的都會是主題色。

**過程中被證偽的一個假設，留著避免下一輪重走**：原本以為差異出在 `.cal-screen { overflow: hidden }`（`calendar.css` 9–13）讓日曆分頁不是捲動容器。**那條規則其實沒有生效** —— `.dp-screen { overflow-y: auto }`（`shell.css` 97–105）同 specificity 但在後面載入，日曆分頁算出來也是 `auto`。只讀原始碼會得到相反結論，必須看計算值。這條 dead rule 目前沒有可見症狀（`.cal-screen` 的 `scrollHeight` 等於 `clientHeight`），但屬同一類遺留，已一併記進 DP-074。

---

## 3. 這一輪沒有涵蓋的部分（DP-032 仍未完成）

以下第一輪 §4 列出的項目**本輪未測**，如實記錄，不得視為通過：

- [ ] **Android Chrome**：安裝橫幅、maskable 圖示在啟動器的裁切、返回鍵行為。**專案擁有者手邊沒有 Android 裝置**，需另覓裝置。
- [ ] **螢幕閱讀器（VoiceOver／TalkBack）**：專案擁有者決定本輪先跳過，後期再補。DP-069（roving tabindex）與 DP-071（`main`／`h1`）都是為螢幕閱讀器做的修正，**至今只在模擬環境驗過**，沒有任何真實螢幕閱讀器走訪過。
- [ ] **實體鍵盤／輔助輸入**：無外接鍵盤。

完成前 DP-032 維持 In Progress。
